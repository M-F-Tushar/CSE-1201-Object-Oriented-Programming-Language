/*
Q3 (a)
Inheritance:

Inheritance allows one class (child) to use properties and methods of another class (parent).

Types:

Public inheritance → public & protected of base stay same in derived.

Protected inheritance → public & protected of base become protected in derived.

Private inheritance → public & protected of base become private in derived.
*/